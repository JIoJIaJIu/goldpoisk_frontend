 block('g-available-in-stores')(
    content()(function() {
        return [
            this.ctx.content.map(function(store) {
                    return [
                    {
                        block: 'g-item-buy-in-shop',
                        store: store.name,
                        url: store.url,
                        price: store.price
                    }
                ];
            }),
            {
                block: 'clear'
            }
        ]
    })
)
block('g-menu').match()(
    js()(true),
    content()(function() {
        return {
            elem: 'list',
            tag: 'ul',
            content: [{
                block: 'g-menu-item',
                mods: { type: 'search' }
            }].concat(applyNext())
        };
    })
)

block('g-button')(
    tag()('button'),
    match(this.ctx.href)(
        tag()('a'),
        attrs()(function () {
            return {
                href: this.ctx.href || '#',
                target: (this.ctx.newPage) ? '_blank' : null
            }
        })
    )
)
